<form action="{{ path('admin.articles.delete', {id: article.id}) }}" method="POST" onsubmit="return confirm('êtes-vous sur de supprimer cette article ?')">
    <input type="hidden" name="token" value="{{ csrf_token('delete' ~ article.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>